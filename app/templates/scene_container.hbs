<li class="tab-pane" id="{{id}}" data-model-id="{{id}}">
  <div class="row">
    <div class="col-md-3">
      <ul class='nav nav-pills nav-stacked layers-list'></ul>
    </div>
    <div class="col-md-9">
      <div class="canvas-container">
        <div class="grid" style='width:{{width}}px;height:{{height}}px;background-size:{{tileSizeX2}}px {{tileSizeX2}}px;background-position: 0 0, {{tileSize}}px {{tileSize}}px;'></div>
        <canvas width='{{width}}' height='{{height}}'></canvas>
      </div>
    </div>
  </div>
</li>
